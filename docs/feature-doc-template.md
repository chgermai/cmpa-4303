# {{ Feature name }} notes

Context for {{ what was added and where — e.g. the button added to
`index.html` and `css/style.css` }}. Not linked from the published site — kept as
repo-local documentation.

## Request

{{ The original ask, in one or two sentences. }}

## Approach

- {{ Where the markup / code lives and what identifies it (id, class,
  filename). }}
- {{ How it works, step by step. Note any single source of truth it reads
  from so it stays in sync. }}
- {{ Failure / edge-case behavior. }}

## Styling

- {{ New CSS classes and which existing `:root` tokens they reuse. Delete
  this section if the feature adds no styles. }}

## Caveat

{{ Anything that will bite later: runtime requirements, browser support,
assumptions about how the site is served. Delete if none. }}
